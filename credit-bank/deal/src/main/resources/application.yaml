spring:
  application:
    name: deal
  datasource:
    url: jdbc:postgresql://localhost:5432/neoflex-credit-bank-db
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8082